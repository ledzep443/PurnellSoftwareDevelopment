<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :url, class: 'form-label' %>
      <%= form.text_field :url, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :github, class: 'form-label' %>
      <%= form.text_field :github, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :languages, class: 'form-label' %>
      <%= form.text_field :languages, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :framework, class: 'form-label' %>
      <%= form.text_field :framework, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <% if project.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', project, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if project.persisted? %>
      <%= link_to "Cancel", project, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", projects_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
